[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "formicai"
version = "0.2.0"
description = "FormicAI Vision: motion analysis and ant detection dataset tooling."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26,<2.0",
    "opencv-python>=4.9,<5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
ml = [
    "ultralytics>=8.4.115,<9.0",
]

[project.scripts]
formicai = "formicai.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
